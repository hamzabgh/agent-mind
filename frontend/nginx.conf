# Minimal SPA config for the Docker image (see Dockerfile). For a bare-metal
# nginx setup with API reverse-proxying too, use ../nginx.conf.example instead.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $uri /index.html;
    }
}
